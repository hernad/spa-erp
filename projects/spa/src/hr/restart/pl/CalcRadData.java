package hr.restart.pl;

import hr.restart.util.Aus;

import java.math.BigDecimal;

public class CalcRadData {
  String cradnik;
  BigDecimal bruto = Aus.zero2;
  BigDecimal osndop = Aus.zero2;
  public CalcRadData(String _cradnik) {
  }
}
